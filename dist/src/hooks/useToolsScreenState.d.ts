/**
 * Hook pour gérer l'état des écrans dans ToolsSlot
 * @returns Les états et actions nécessaires pour les écrans dans ToolsSlot
 */
export declare const useToolsScreenState: () => {
    activeScreenId: string;
    isDetached: boolean;
    multiScreen: boolean;
};

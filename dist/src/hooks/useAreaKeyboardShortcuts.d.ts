import { KeyboardShortcut } from "../store/registries/keyboardShortcutRegistry";
/**
 * Hook to register keyboard shortcuts for an area type
 */
export declare function useAreaKeyboardShortcuts(areaType: string, shortcuts: KeyboardShortcut[]): void;
